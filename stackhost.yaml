# StackHost configuration file

# Version: 2.0.0

user:
  name: Nikhil Gurjar
  github: Whitedevil20135

runtime:
  image: python:3.11-slim

commands:
  package: libssl-dev libffi-dev
  build:
    - pip install --upgrade pip
    - pip install -r requirements.txt
  start: python bot.py

repository:
  branch: main
  auto_deploy: true

env:
  - BOT_USERNAME:DemonSlayerXbot
  - OWNER_ID:1214273889
  - PORT:10000
  # Configure BOT_TOKEN and MONGO_URL in StackHost's secret manager.
